/**
 * @param color cor do elemento;
 * @param onClick <?> ação de click do botão;
 * @param size tamanho do botão
 */
export default interface ArrowButtonProps {
    color?: string
    onClick?: () => void;
    size: string
}