/**
 * @param logo Componente para manter o logo da navbar;
 * @param navItens Componente dos itens de navegação do menu;
 */
export default interface NavbarProps{
    logo?: React.ReactNode
    navItens?: React.ReactNode
}